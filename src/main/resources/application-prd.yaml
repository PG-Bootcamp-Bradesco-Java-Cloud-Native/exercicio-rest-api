spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    driverClassName: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    open-in-view: false
    hibernate:
      ddl-auto: create-drop